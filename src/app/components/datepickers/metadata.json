{
  "examples": {
    "basic": "Inline",
    "input": "Input popup",
    "format": "Date format",
    "filter": "Disabling dates",
    "disabled": "Disabling parts of the datepicker",
    "validation": "Validation",
    "config": "Global configuration"
  }
}
