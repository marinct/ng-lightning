.slds-text-title--caps.slds-m-bottom--small Default
ngl-pagination([(page)]="pageDefault", total="62")
ngl-pagination([(page)]="pageBoundary", total="42", boundaryLinks)
.slds-text-title--caps.slds-m-bottom--small.slds-m-top--large Limited visible options
.slds-text-title--caps-normal.slds-m-bottom--x-small Showing {{pagination.start}} - {{pagination.end}} of {{total}}
ngl-pagination([(page)]="page", [total]="total", limit="5", boundaryNumbers="1", #pagination)
| Total:
input([(ngModel)]="total")
